# Review Gate (Baseline)

This baseline kit does not require any AI/automation reviewer.

Recommended practice:
- Keep PRs small and plan-scoped.
- Run `npm test` locally before requesting review.
- Use the plan gates (S98/S99) inside the plan file as the single source of truth for readiness.

