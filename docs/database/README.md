# Database Documentation (Optional)

This folder is reserved for database-specific notes and generated audit outputs.

- `schema-audit/` - templates/placeholders for per-project schema inventory/audit output.

If your project does not use a database, you can ignore this folder.

