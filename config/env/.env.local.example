# Local environment template (no secrets).
#
# Copy to `config/env/.env.local` locally (untracked). Use this for local
# development runs.

APP_NAME=<your-app-name>
NODE_ENV=development

# Connectivity (optional)
DATABASE_URL=
API_BASE_URL=http://127.0.0.1:3000
