# Generic environment template (no secrets).
#
# Copy to `config/env/.env` (untracked) or point `ENV_FILE` to a specific file
# such as `config/env/.env.local` or `config/env/.env.cloud`.

# App identity
APP_NAME=<your-app-name>
NODE_ENV=development

# Connectivity (optional; set only what your project uses)
DATABASE_URL=
API_BASE_URL=

# Example feature flags (optional)
FEATURE_EXAMPLE_ENABLED=false
