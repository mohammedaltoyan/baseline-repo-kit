# Cloud environment template (no secrets).
#
# Copy to `config/env/.env.cloud` locally (untracked). Use this for cloud
# staging/production-like runs in your project.

APP_NAME=<your-app-name>
NODE_ENV=production

# Connectivity (optional)
DATABASE_URL=
API_BASE_URL=
