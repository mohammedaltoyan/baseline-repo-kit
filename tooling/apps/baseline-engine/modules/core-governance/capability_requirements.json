{
  "requires": ["rulesets", "repo_variables"],
  "degrade_strategy": "warn",
  "remediation": {
    "rulesets": "Enable repository rulesets and use a token with admin scope to enforce generated governance policies.",
    "repo_variables": "Allow repository variables API access for policy and guardrail automation."
  }
}
