{
  "requires": ["rulesets"],
  "degrade_strategy": "warn",
  "remediation": {
    "rulesets": "Grant repository administration permissions and ensure rulesets are enabled for this repository type.",
    "merge_queue": "Enable merge queue in repository rulesets or upgrade to a GitHub plan that supports merge queue."
  }
}
